# 🤖 ภาพรวมระบบเทรน AI พยากรณ์ความต้องการห้อง

สรุปแบบย่อว่าโค้ดเทรนโมเดล (`ml/saved/forecast.py`) ทำงานยังไงตั้งแต่ต้นจนจบ — เอกสารนี้พูดถึง "โครงสร้าง" ภาพรวม ถ้าอยากรู้รายละเอียดการแก้ปัญหา LSTM โดยเฉพาะ ดูที่ `LSTM_FIX_SUMMARY.md`

---

## 1. ภาพรวม pipeline

```
Booking (DB)  ──►  รวมเป็น "ชั่วโมงจองรวมต่อวัน" ต่อห้อง
                        │
                        ▼
                Feature Engineering
              (วัน/เดือน, ค่าย้อนหลัง, ค่าเฉลี่ยเคลื่อนที่, ตารางเรียน ฯลฯ)
                        │
                        ▼
        ┌───────────────┼───────────────┐
        ▼               ▼               ▼
     LightGBM         XGBoost          LSTM
    (โมเดลรอง)       (โมเดลรอง)      (โมเดลหลัก)
        └───────────────┼───────────────┘
                        ▼
              รวมผล (weighted ensemble)
                        │
                        ▼
          กระจายค่าพยากรณ์ "รายวัน" ไปเป็น "รายชั่วโมง"
          (ใช้สัดส่วนการใช้งานตามชั่วโมงในอดีตของห้องนั้น)
                        │
                        ▼
              ตาราง DemandForecast (DB)
                        │
                        ▼
              API เสิร์ฟให้ Frontend อ่านต่อ
```

**จุดสำคัญ:** โมเดลพยากรณ์ **"ยอดจองรวมทั้งวัน" ของแต่ละห้อง** ไม่ได้พยากรณ์ทีละชั่วโมงตรงๆ — ค่ารายชั่วโมงเป็นการ "กระจาย" ค่ารายวันนั้นออกไปอีกที โดยอิงจากพฤติกรรมการใช้งานในอดีต (เช่น ห้องนี้ปกติคนจองช่วงบ่ายเยอะกว่าเช้า)

---

## 2. โมเดลทั้ง 3 ตัว ทำหน้าที่อะไร

| โมเดล | บทบาท | จุดเด่น |
|---|---|---|
| **LSTM** | โมเดลหลัก (primary) | จับ pattern ตามลำดับเวลา/ฤดูกาลได้ดี |
| **LightGBM** | โมเดลรอง (support) | เรียนรู้ feature เชิงโครงสร้างเร็ว แม่นกับข้อมูลน้อย |
| **XGBoost** | โมเดลรอง (support) | คล้าย LightGBM แต่ tuning คนละแบบ ช่วย cross-check กัน |

ผลจากทั้ง 3 โมเดลจะถูก **ผสมกันแบบถ่วงน้ำหนัก (weighted ensemble)** — ค่าเริ่มต้นประมาณ LSTM 20% / LGB 40% / XGB 40% แล้วปรับน้ำหนักอัตโนมัติต่อห้องตามความแม่นยำจริงของแต่ละโมเดลในห้องนั้น (บางห้องอาจได้น้ำหนัก LSTM สูงกว่าถ้า LSTM แม่นกว่าในห้องนั้นๆ)

---

## 3. Feature ที่ใช้เทรน (สรุปเป็นหมวด)

- **ปฏิทิน** — วันในสัปดาห์, เดือน, ไตรมาส, สัปดาห์ที่เท่าไหร่ของปี, วันหยุดสุดสัปดาห์หรือไม่
- **ค่าย้อนหลัง (lag)** — ยอดจองเมื่อ 1/2/3/7/14/21/28 วันก่อน และย้อนหลัง 1 ปี (จับ pattern ซ้ำรอบปี เช่น ช่วงสอบ)
- **ค่าเฉลี่ย/ส่วนเบี่ยงเบนเคลื่อนที่** — เฉลี่ย 3/7/14/28 วันล่าสุด
- **แนวโน้มการเปลี่ยนแปลง** — เพิ่ม/ลดจากวันก่อน, จากสัปดาห์ก่อน
- **ความเป็นฤดูกาล (Fourier)** — จับรอบสัปดาห์ (7 วัน) และรอบปี (365 วัน)
- **ตารางเรียนทั้งเทอม (TermBooking)** — ห้องนี้มีคาบเรียนวันนั้นกี่ชั่วโมง, อยู่ในช่วงเทอมไหม, ใกล้ปิดเทอมแค่ไหน — ใช้เป็นสัญญาณเสริมว่าห้องน่าจะไม่ว่างช่วงไหน

---

## 4. เทรนแยกทีละห้อง

- โมเดล 3 ตัว **เทรนแยกต่อห้อง** (ไม่ใช่โมเดลเดียวพยากรณ์ทุกห้อง) — แต่ละห้องมีไฟล์โมเดลของตัวเอง
- ห้องที่มีข้อมูลจองน้อยเกินไป (< 15 รายการ, < 30 วัน, < 14 วันที่ไม่ซ้ำกัน) จะถูก **ข้าม** ไม่เทรน
- ในทางปฏิบัติตอนนี้มีแค่ ~8 ห้องที่มีประวัติการจองมากพอให้เทรนจริง

---

## 5. ผลลัพธ์ที่ได้ (เก็บที่ไหน)

ต่อ 1 ห้อง จะได้ไฟล์:
- `saved_models/{room_id}/{room_id}_lgb.pkl`, `_xgb.pkl` — โมเดล LightGBM/XGBoost
- `saved_models/{room_id}/{room_id}_lstm.keras` + `_lstm_scaler.pkl` — โมเดล LSTM + ตัวปรับสเกลข้อมูล
- `saved_meta/{room_id}_meta.pkl` — metadata (threshold, น้ำหนัก ensemble, metrics, ประวัติการเทรน ฯลฯ)

ไฟล์เหล่านี้อยู่ในเครื่อง server เท่านั้น (ไม่ได้ push ขึ้น GitHub — ดู `.gitignore`)

จากนั้นค่าพยากรณ์รายชั่วโมงจะถูกเขียนลงตาราง **`DemandForecast`** โดยจัดกลุ่มเป็น 4 ระดับตาม threshold ที่ปรับอัตโนมัติต่อห้อง:

| ระดับความต้องการ | สถานะที่ผู้ใช้เห็น |
|---|---|
| ต่ำ (low) | 🟢 ยังว่าง |
| ปานกลาง (medium) | 🟡 ควรจอง |
| สูง (high) | 🟠 รีบจอง |
| เร่งด่วน (urgent) | 🔴 ควรจองตอนนี้เลย |

---

## 6. วิธีสั่งเทรน

- **อัตโนมัติทุกวัน** — `booking/scheduler.py` สั่ง `python ml/saved/forecast.py --retrain` เวลาตี 3 ทุกวัน (เทรนใหม่ทั้งหมด + สร้างค่าพยากรณ์ใหม่)
- **ไม่ใส่ flag** — ใช้โมเดลที่เทรนไว้แล้ว มาคำนวณค่าพยากรณ์ใหม่เฉยๆ (ไม่เทรนซ้ำ เร็วกว่า)
- **`--param-set {A-E}`** — เลือกชุด hyperparameter ทดลอง (ค่า default คือ D) ใช้สำหรับเทียบประสิทธิภาพหลายแบบ ไม่ใช่ของจริงที่ใช้ production ทุกชุด
- flag อื่นๆ: `--update-fac` (อัปเดตชื่ออุปกรณ์), `--import-excel` (นำเข้าข้อมูลจริงจาก Excel แล้วเทรน), `--boost` (ปรับ threshold แล้วคำนวณใหม่), `--show-metrics` (ดู metrics ที่เทรนไว้)

---

## 7. ไฟล์อื่นๆ ที่เกี่ยวข้อง (เผื่ออยากรู้ว่าใช้ทำอะไร)

| ไฟล์ | ใช้ทำอะไร |
|---|---|
| `param_sets.py` | เก็บชุด hyperparameter A-E ที่ใช้ร่วมกันทุกสคริปต์ |
| `plotting.py` / `generate_plots.py` | สร้างกราฟ training curve |
| `metrics_report.py` | พิมพ์สรุป metrics ของแต่ละห้อง/โมเดล |
| `train_from_excel.py` / `test_from_excel.py` | เทรน/ทดสอบโดย split ข้อมูลจาก Excel ตายตัว |
| `train_lstm_only.py` | เทรนเฉพาะส่วน LSTM อย่างเดียว |
| `compare_hyperparams.py` | เทียบผลลัพธ์ระหว่างชุด A-E |
| `run_abcd_experiment.sh` | สคริปต์รันทดลองไล่ทีละชุด A→B→D→E→C อัตโนมัติ |

ดูรายละเอียดโฟลเดอร์/ไฟล์ทดลองเพิ่มเติมได้ที่ `ml/saved/README.md`

---

## 8. เชื่อมกับเว็บแอปยังไง

API ที่ผู้ใช้เรียก **ไม่ได้โหลดโมเดลหรือคำนวณสดๆ** เลย — แค่อ่านค่าที่คำนวณไว้แล้วจากตาราง `DemandForecast` (`booking/views.py`) การเทรน/พยากรณ์จริงเกิดแค่ตอน scheduler สั่งรันตอนตี 3 เท่านั้น ทำให้ API ตอบเร็ว ไม่ต้องรอโมเดลคำนวณตอน request เข้ามา
